Position:
  # 经度
  LNG: '113.264434'
  # 纬度
  LAT: '23.129162'

# 可配置时间偏移，比如日出提前30分钟配置 30，延后30分钟配置 -30
Time_offset:
  # 日出时间偏移
  sunrise_offset_minutes: 0
  # 日落时间偏移
  sunset_offset_minutes: 0

Theme_path:
  # 浅色主题路径，留空则使用系统默认主题
  light_theme_path: ''
  # 深色主题路径，留空则使用系统默认主题
  dark_theme_path: ''

Wallpaper_path:
  # 浅色主题对应的壁纸路径，留空则从主题文件读取
  light_wallpaper_path: ''
  # 深色主题对应的壁纸路径，留空则从主题文件读取
  dark_wallpaper_path: ''
